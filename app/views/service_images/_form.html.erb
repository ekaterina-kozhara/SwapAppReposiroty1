<%= form_with(model: service_image, local: true) do |form| %>
  <% if service_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_image.errors.count, "error") %> prohibited this service_image from being saved:</h2>

      <ul>
        <% service_image.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.number_field :service_id %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
